# Cryotherapy Rejuvenate

The Next.js app. From the repo root:

```bash
cd site
npm install
npm run dev
```

On Vercel, set the project's **Root Directory** to `site`. Full notes are in
the [root README](../README.md#deploy-on-vercel).
